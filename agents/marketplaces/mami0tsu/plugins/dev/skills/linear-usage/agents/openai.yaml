interface:
  display_name: "Linear Usage"
  short_description: "Linear MCP で ticket と依存関係を安全に操作する"
  default_prompt: "Use $linear-usage to read or update Linear tickets through the shared ticket contract."

dependencies:
  tools:
    - type: "mcp"
      value: "linear"
      description: "Linear issue の取得、作成、更新、relation 操作"
      transport: "streamable_http"
      url: "https://mcp.linear.app/mcp"
